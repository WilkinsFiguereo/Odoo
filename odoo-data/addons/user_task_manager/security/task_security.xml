<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <record id="user.task_own_rule" model="ir.rule">
        <field name="name">Solo ver mis tareas</field>
        <field name="model_id" ref="model_user_task"/>
        <field name="domain_force">[('user_id', '=', user.id)]</field>
        <field name="groups" eval="[(4, ref('5y'))]"/>
    </record>

</odoo>
